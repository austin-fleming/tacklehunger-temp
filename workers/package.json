{
  "name": "worker",
  "version": "0.0.3",
  "description": "Cloudflare Worker scripts - deploys & serves static frontend assets from KV store",
  "private": true,
  "scripts": {
    "clean": "rm -rf .next out dist",
    "codegen": "cd .. && yarn codegen",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.1.1",
    "toucan-js": "^2.4.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^2.2.1",
    "ts-loader": "^8.1.0",
    "typescript": "^4.2.4",
    "webpack": "^5.31.0",
    "webpack-cli": "^4.6.0"
  }
}
